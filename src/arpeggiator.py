import numpy as np\n\n\nclass Arpeggiator:\n    \"\"\"Generate melodic arpeggio patterns as spectrogram tiles.\n\n    This module creates frequency-time tiles that represent arpeggiated chord\n    patterns. It can be used to overlay harmonic movement on the ambient\n    diffusion output, adding subtle melodic interest. The arpeggiator\n    generates a 2D array where each row corresponds to a frequency bin and\n    each column to a time step. Notes are placed as short bursts of energy\n    at their fundamental frequencies, with optional harmonics.\n\n    Parameters\n    ----------\n    sample_rate : int\n        Sample rate of the audio (used to map frequency bins).\n    fft_size : int\n        FFT size used for the spectrogram (number of frequency bins).\n    hop_length : int\n        Hop length in samples between time frames.\n    \"\"\"\n\n    def __init__(self, sample_rate=22050, fft_size=1024, hop_length=256):\n        self.sample_rate = sample_rate\n        self.fft_size = fft_size\n        self.hop_length = hop_length\n        self.freq_bins = fft_size // 2 + 1\n\n    def _freq_to_bin(self, freq):\n        \"\"\"Convert a frequency in Hz to the nearest FFT bin index.\"\"\"\n        return int(round(freq * self.fft_size / self.sample_rate))\n\n    def _note_freq(self, midi_note):\n        \"\"\"Convert a MIDI note number to frequency in Hz.\"\"\"\n        return 440.0 * (2.0 ** ((midi_note - 69) / 12.0))\n\n    def generate(self, chord, duration_steps, bpm=60, pattern='up',\n                 velocity=0.8, harmonics=(1, 2, 3), decay=0.6, seed=None):\n        \"\"\"Generate an arpeggio spectrogram tile.\n\n        Parameters\n        ----------\n        chord : list of int\n            MIDI note numbers forming the chord (e.g., [60, 64, 67]).\n        duration_steps : int\n            Number of time frames in the tile.\n        bpm : int, optional\n            Tempo in beats per minute for note timing.\n        pattern : str, optional\n            Arpeggio pattern: 'up', 'down', 'updown', 'random'.\n        velocity : float, optional\n            Amplitude of each note burst (0.0 to 1.0).\n        harmonics : tuple, optional\n            Multipliers for overtone series (e.g., (1, 2, 3) for fundamental,\n            octave, and twelfth).\n        decay : float, optional\n            Exponential decay factor for each note's energy over time.\n        seed : int, optional\n            Random seed for reproducibility (used only for 'random' pattern).\n\n        Returns\n        -------\n        np.ndarray\n            2D float array of shape (freq_bins, duration_steps), ready to be\n            blended with the diffusion output.\n        \"\"\"\n        rng = np.random.default_rng(seed)\n        tile = np.zeros((self.freq_bins, duration_steps), dtype=np.float32)\n\n        # Determine note sequence based on pattern\n        if pattern == 'up':\n            indices = range(len(chord))\n        elif pattern == 'down':\n            indices = range(len(chord) - 1, -1, -1)\n        elif pattern == 'updown':\n            indices = list(range(len(chord))) + list(range(len(chord) - 2, 0, -1))\n        elif pattern == 'random':\n            indices = rng.integers(0, len(chord), size=duration_steps)\n        else:\n            raise ValueError(f\"Unknown pattern: {pattern}\")\n\n        # Time between notes in frames\n        beat_frames = int(60 / bpm * self.sample_rate / self.hop_length)\n        if beat_frames <= 0:\n            beat_frames = 1\n\n        # Place each note\n        for step, idx in enumerate(indices):\n            if step >= duration_steps:\n                break\n            note = chord[idx % len(chord)]\n            freq = self._note_freq(note)\n            bin_idx = self._freq_to_bin(freq)\n\n            for h in harmonics:\n                harmonic_bin = bin_idx * h\n                if harmonic_bin >= self.freq_bins:\n                    continue\n                # Amplitude decreases with harmonic order\n                amp = velocity / h\n                # Exponential decay over time\n                for t in range(step, min(step + beat_frames, duration_steps)):\n                    time_since_start = t - step\n                    env = np.exp(-decay * time_since_start / beat_frames)\n                    tile[harmonic_bin, t] += amp * env\n\n        # Normalize to prevent clipping when blending\n        max_val = np.max(np.abs(tile))\n        if max_val > 0:\n            tile /= max_val\n            tile *= velocity\n\n        return tile\n